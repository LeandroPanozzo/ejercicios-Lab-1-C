#include <stdio.h>
#include<math.h>

int main(int argc, char *argv[]) {
	int a, potencia, elevado;
	
	a=4;
	potencia=2;
	//elevado= pow(a,2);
	elevado=sqrt(a);
	printf("%d",elevado);
	return 0;
}
//para hacer raiz cuadrada uso la funcion sqrt
