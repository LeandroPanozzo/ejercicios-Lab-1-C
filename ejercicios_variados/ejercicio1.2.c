#include <stdio.h>
#include <conio.h>
int main()
{
    int A;                                   // Definimos el entero A
    printf("\nIngrese un entero: ");         // Mostramos por pantalla, "Ingrese un entero"
    scanf("%d", &A);                         // Guardamos lo ingresado por consola en la variable A
    printf("El entero ingresado es: %d", A); // Mostramos el entero ingresado

    return 0;
}
